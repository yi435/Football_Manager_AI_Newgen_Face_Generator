Drop your screenshots in this folder and reference them from README.md.

Expected:
  app-ui.png        — the app UI while generating
  faces-in-fm.png   — generated faces visible in Football Manager

Keep them reasonably small (JPG/PNG, under ~500 KB each).
Until the files exist, the README image links will simply not render.
